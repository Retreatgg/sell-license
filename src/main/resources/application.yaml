spring:
  datasource:
    url: jdbc:postgresql://localhost:1234/sell_licences_db?stringtype=unspecified
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver

  liquibase:
    change-log: classpath:/db/changelog/master.yaml

server:
  port: 8081
  address: 0.0.0.0

app:
  defaultPhoneNumber: +996550150176
  markup:
    percent: 20